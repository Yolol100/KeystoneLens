name: Retired temporary sync helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary sync helper retired; main already contains the verified source and rebuilt artifacts."
